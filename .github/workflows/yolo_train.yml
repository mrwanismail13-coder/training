name: Train YOLOv11 from Local Dataset

on:
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Ultralytics
        run: |
          python -m pip install --upgrade pip
          pip install ultralytics

      - name: Run YOLOv11 Training
        run: |
          python -c "
          from ultralytics import YOLO
          import os
          
          model = YOLO('yolo11n.pt')
          yaml_path = 'dataset/data.yaml'
          
          if os.path.exists(yaml_path):
              print('Found data.yaml! Starting 1-Epoch Test...')
              # تشغيل لفة واحدة فقط للتأكد من مسار حفظ الملفات والرفع
              model.train(
                  data=yaml_path, 
                  epochs=1, 
                  imgsz=640, 
                  device='cpu', 
                  cache=True,
                  project='runs/detect',
                  name='my_train'
              )
          else:
              raise FileNotFoundError(f'Critical Error: {yaml_path} does not exist!')
          "

      # خطوة الرفع المؤمنة والمباشرة من المجلد الثابت
      - name: Upload Trained Weights
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yolov11-8ballpool-local-weights
          path: runs/detect/my_train/weights/best.pt
